{{#each my_teams as |team|}}
  <a href='/team/{{team.id}}'>
    <div class='card-container'>
      <div class='border border-info card text-white bg-dark' style='width: 18rem'>
        <div class='card-header'>Name: {{team.team_name}}</div>
      </div>
    </div>
  </a>
{{/each}}